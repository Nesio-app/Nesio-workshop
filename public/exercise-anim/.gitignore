# 动作定格帧目录。第三方版权媒体(licensed,如 Gym visual)默认忽略——
# 往 GitHub 推 = 再分发,可能侵权。自有/公共领域动图用 `!<id>/` 显式放行。
*/
!.gitignore
!lying-leg-raise/
# catalog/ = exercises-dataset 演示 GIF(Gym visual)。私有仓库 + 私有部署,自用放行。
# 若仓库转公开或部署公开,须移除本行并清空 catalog/(否则构成再分发)。
!catalog/
