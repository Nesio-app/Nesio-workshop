Quiet glass text field. Glows softly on focus, never with a hard ring.

```jsx
<Input label="显示名称" placeholder="输入你的名字" />
<Input label="现在的想法是…" multiline rows={3} />
```

`label`, `hint`, `multiline`, `rows`. Inherits theme via glass tokens.
