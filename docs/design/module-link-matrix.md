# 板块关联矩阵（Module Link Matrix）

> **这份文档回答一个问题：一个东西在 A 板块被记下，B 板块能不能看见它、用得上它。**
>
> 起因（2026-07-30）：四个 use case 问出了同一个坑——
> ① Plaid 交易上手动关联了人和附件，关系页看不到、记忆库搜不到；
> ② 美食「记一条」拍的小票，不会跟银行流水对账；
> ③ 机票邮件能关联到**已存在**的行程，但不会新建行程；
> ④ 资产上传的发票不触发识别，金额要手打。
>
> 一条一条查都是「这个功能忘了做关联」，合起来看才是一件事：
> **关联的地基只覆盖记忆图内部，而一半的业务对象根本不在记忆图里。**

---

## 一、三套关联机制（现状）

| | 机制 | 存在哪 | 谁看得见 |
|---|---|---|---|
| **A** | `LifeNode.relations` | 记忆图内部，双向可写 | 记忆详情、问一问、图谱、时间线 |
| **B** | 旁挂 overlay（模块自己的表） | 各自的 localStorage / IDB key | **只有写它的那一个屏幕** |
| **C** | 无 | — | — |

A 机制目前只有一处做到了「自动 + 双向 + 可解释」：`lib/portal/plan-links.ts`（行程 ↔ 确认邮件，
三条确定性锚点：航班号 / 机场对+日期 / PNR）。它能成立，正因为**两头都是 LifeNode**。

B 机制的例子：`tx-annotations`（交易↔人/附件）、`finance-sources.linkedBankTxId`（小票↔流水）、
`Expense.assetId` + `CareRecord.expenseId`（资产↔支出）。每一处单看都对，合起来是七八张互不相通的小表。

## 二、四色状态

| 记号 | 含义 |
|---|---|
| 🟢 | 已连，**两头都看得见** |
| 🟡 | 连了，但**只有一头看得见**（或只在某一个屏幕内有效） |
| 🔴 | 该连没连 |
| ⚪ | 有意不连（须写明理由） |
| ❓ | 未核实 |

## 三、每格必答的五问

1. **进什么** —— 这个板块产生的东西是 LifeNode，还是自己的表？
2. **凭什么去重** —— 有稳定 id 吗？重复操作会不会堆？
3. **关联谁** —— 该跟哪些板块连？现在走 A / B / C？
4. **看得见吗** —— 关联建立后，**两头**都能看到吗？
5. **删了会怎样** —— 附件、反向引用、外部 id 墓碑。

## 四、16 个板块

洞察页 14 个 tab + 宫格 2 个（`InsightsSheet.tsx:424-435`）：

`回看(reflection)` · `成长(growth)` · `蒙太奇(montage)` · `健康(health)` · `运动(fitness)` ·
`时间线(timeline，含足迹/旅行)` · `日程(schedule)` · `财务(finance)` · `物品(inventory)` ·
`衣橱(wardrobe)` · `关系(relationships)` · `资产(tesla)` · `生活模型(living)` · `运维(admin)` ·
`家务(chores)` · `美味(cooking)`

---

# 样板 · 财务（finance）

## 五问

### ① 进什么

| 业务对象 | 存储 | 是 LifeNode 吗 |
|---|---|---|
| 银行流水 `BankTx` | `nesio-bank-tx-v1` | ❌ |
| 支出聚合 `Expense`（小票/旅行/手动/Tesla） | `nesio-expenses-v1`（IDB blob） | ❌ |
| 手动资产 `ManualAsset` | `nesio-fin-assets-v1` | ❌ |
| 持仓 `Holding` | `nesio-fin-holdings-v1` | ❌ |
| 定期账单 `RecurringCharge` | `nesio-bank-recur-v1` | ❌ |
| 交易批注（人 / 附件 / 备注） | `nesio-fin-tx-annotations-v1` | ❌（overlay） |
| 资产照料记录 | `nesio-asset-care-v1` | ❌ |
| 对账否决对 | `nesio-receipt-match-rejected-v1` | ❌ |
| **月报** | — | ✅ `finance-report.ts:209`（`type:'event'`，tags `财务`/`月报`） |

**九个对象里只有月报进了记忆图。** 这一条决定了后面所有 🔴。

### ② 凭什么去重

| 对象 | 幂等键 | 状态 |
|---|---|---|
| BankTx | Plaid transaction_id | ✅ |
| Expense（小票） | `camera:日期:指纹`（`CameraSheet.tsx:824`） | ⚠️ 是自造键，跟 `externalKey()` 无关，也不是 node id |
| 定期账单 | `merchantKey` | ✅ |
| 交易批注 | `tx.id` | ✅ |
| 资产照料 | 自增 id | ⚠️ 同一张发票传两次会有两条 |

### ③④ 关联谁 / 看得见吗

| 财务 ↔ | 状态 | 现状与锚点 |
|---|---|---|
| **关系（人）** | 🟡 | `tx-annotations.people` 存的是人名 key（`tx-annotations.ts:25`），**全仓只有 `FinanceTab.tsx` 读它**。Linda 的关系页看不到这笔钱 |
| **附件** | 🟡 | 附件本体在 `local-file-store`，批注里只留 assetId。**不在 `node.assets` 体系**，问一问/记忆详情都取不到 |
| **记忆 / 时间线 / 问一问** | 🔴 | 交易、支出、资产全不在图里。`MemoryTab` 从不读 `loadBankTx`（grep 零命中）→ **记忆库搜不到任何一笔消费** |
| **物品（相机小票）** | 🟡 | 拍小票 → 既落 `object` 节点又落 Expense（`CameraSheet.tsx:817`），但 `sourceRef` 是指纹不是 node id → **两者之间没有可导航的链接** |
| **资产** | 🟡 | `addManualEntry({assetId, assetCostKind})` + `expenseId` 回写照料记录（`AssetsPanel.tsx:311/325`）——**双向、删不留孤儿账，做得对**。但财务页点开这笔税费看不到发票 |
| **行程（旅行）** | 🟡 | `travel-trips.ts:651` → `addReceiptExpense({includeInFinance:true})`，旅行小票进财务 ✅ 单向；财务侧看不到「这是哪次旅行」 |
| **健康（就诊费用）** | 🔴 | `HealthRecordSheet` 已有 `price`/`currency` 字段（bug3 p41），**那笔钱不进财务** —— 医疗支出在月度汇总里是隐形的 |
| **日程** | 🔴 | 定期账单有续费日，日历里没有它；账单邮件也不对账 |
| **家务（零花钱）** | ⚪ | `finance-sources.ts:4` 明写「家务零花钱（play money）故意不进这里」——**有意，理由已记录** |
| **衣橱** | ❓ | 衣物有无价格字段、是否该进支出，待核 |
| **美味** | 🔴 | `addMeal()`（`meals.ts:35`）只存营养，**无金额字段** → 永远进不了 `receiptMatchCandidates` |
| **成长 / 回看 / 洞察** | 🟢 | 月报进图，可搜、可引用 |

### ⑤ 删了会怎样

- 删交易批注 → `deleteLocalFile` 有调（`tx-annotations.ts:14`）✅
- 删照料记录 → `expenseId` 回写让账能一起清 ✅
- 删记忆节点 → `deleteLifeNode`（`life-graph.ts:1137`）**不清附件、不清 dHash 索引、不清反向引用** ❌
- 导入类记忆删掉 → **无墓碑，下次同步复活** ❌

## 财务这一格的结论

对账、资产持有成本、旅行小票并账——**确定性的钱路都通了，而且写得扎实**
（`receipt-match.ts` 的 ±1% + ±3 天 + 否决记忆是好设计）。

断的全是**同一类**：钱与「人 / 附件 / 记忆」之间。因为那三样活在记忆图里，而钱不在。

---

# 其余 15 格

> 按同一份五问推进，逐格补齐后在文末汇总成 16×16 矩阵。
> **未完成的格子保持空白，不许拿猜测填。**

- [ ] 关系（relationships）
- [ ] 健康（health）
- [ ] 物品（inventory）
- [ ] 衣橱（wardrobe）
- [ ] 时间线 / 足迹 / 旅行（timeline）
- [ ] 日程（schedule）
- [ ] 资产（tesla）
- [ ] 美味（cooking）
- [ ] 家务（chores）
- [ ] 运动（fitness）
- [ ] 成长（growth）
- [ ] 回看（reflection）
- [ ] 蒙太奇（montage）
- [ ] 生活模型（living）
- [ ] 运维（admin）

---

# 其余 15 个板块

> 格式同财务样板,但压缩成一格一表 —— 财务那格已经把五问的**推理过程**写全了,
> 这里只记结论和锚点。看不懂某一格为什么这么判,回去看财务那格。

## 回看（reflection）

| 问 | 答 |
|---|---|
| ① 进什么 | **纯派生视图**。回看读的是已有节点(记忆/心情/日程),自己不产生业务对象 |
| ② 去重 | 不适用 |
| ③ 关联谁 | 读所有面 —— 它是记忆图的**消费者**,不是生产者 |
| ④ 看得见吗 | 🟢 它本身就是「看得见」的实现 |
| ⑤ 删了会怎样 | 源节点删了,回看自然少一条。无孤儿 |

**结论 🟢。** 派生视图的正确形态,不需要动。

## 成长（growth）

| 问 | 答 |
|---|---|
| ① 进什么 | 练习记录 / 协议进度(`lib/portal/growth-engine.ts`、`growth-protocols.ts`),**自建表** |
| ② 去重 | 协议 id + 日期 ✅ |
| ③ 关联谁 | 与「回看」「今天」相通(引擎产卡);与记忆图 🔴 —— 练习记录不是 LifeNode |
| ④ 看得见吗 | 🔴 记忆库搜不到「我这个月练了什么」 |
| ⑤ 删了会怎样 | 自建表自成一体,无跨面孤儿 |

**结论 🔴。** 缺一个「月度成长小结」代表节点(照财务月报的做法)。

## 蒙太奇（montage）

| 问 | 答 |
|---|---|
| ① 进什么 | 纯派生:把已有照片/记忆重排成回放。不产生业务对象 |
| ② 去重 | 不适用 |
| ③ 关联谁 | 读图 —— 消费者 |
| ④ 看得见吗 | 🟢 |
| ⑤ 删了会怎样 | 源删了就少一帧。无孤儿 |

**结论 🟢。**

## 健康（health）

| 问 | 答 |
|---|---|
| ① 进什么 | 临床数据已升格 Signal ✅(H1);心情/此刻走 `mood.ts:49` 的 `health_state` 节点 ✅;**月报** `health-report.ts:156` 进图 ✅;Apple Health 指标是自建表 |
| ② 去重 | 心情节点 **无 `externalId`** ⚠️;健康月报 **无 `externalId`** ⚠️(同一个月重生成靠 `existing` 查找兜底,不是幂等键) |
| ③ 关联谁 | 与「关系(人)」🟡:`person-records` 打通了成员维度(P2),但那是 overlay 不是 `relations`;与**财务** 🔴:`HealthRecordSheet` 有 `price`,那笔钱不进财务 —— 医疗支出在月度汇总里隐形;与「美味」🔴:吃了什么和身体指标不相连 |
| ④ 看得见吗 | 心情/月报 🟢;临床明细 🟡 |
| ⑤ 删了会怎样 | 删节点不清附件(化验单 PDF)❌ —— 同财务那条通病 |

**结论 🟡。** 数据进了图,横向的钱和吃没连上。

## 运动（fitness）

| 问 | 答 |
|---|---|
| ① 进什么 | 训练计划/记录,自建表(`training-*`) |
| ② 去重 | 计划 id + 日期 ✅ |
| ③ 关联谁 | 与健康 🟡(指标能看到,训练本身不在图);与记忆 🔴 |
| ④ 看得见吗 | 🔴 「我上周练了什么」搜不到 |
| ⑤ 删了会怎样 | 自建表自成一体 |

**结论 🔴。** 同成长:缺代表节点。

## 时间线 / 足迹 / 旅行（timeline）

| 问 | 答 |
|---|---|
| ① 进什么 | 到访点是自建表;**旅行小票** 走 `travel-trips.ts:651` → `addReceiptExpense` 进财务 ✅;照片/手记本来就是节点 ✅ |
| ② 去重 | 到访点按坐标+时间窗 ✅;旅行小票用 Expense 的指纹键 ⚠️ |
| ③ 关联谁 | 与财务 🟡(单向:旅行 → 财务,财务侧看不到「这是哪次旅行」);与地点 🟢;与记忆 🟢(照片/手记本就是节点) |
| ④ 看得见吗 | 🟡 —— 单向那半看不见 |
| ⑤ 删了会怎样 | 删行程不清它带进财务的那几笔 ❌ |

**结论 🟡。** 这一格的问题是**方向**,不是有没有连。

## 日程（schedule）

| 问 | 答 |
|---|---|
| ① 进什么 | 日历事件进图 ✅(`connector-sync.ts` 有 `calendarId` 去重);提醒是派生 |
| ② 去重 | `calendarId` + 同批 `seenThisRun` ✅(已被 `connector-idempotency` 契约钉住) |
| ③ 关联谁 | 与**邮件** 🟢(`plan-links.ts` 的确定性连线,R1 之后是原子双向);与**财务** 🔴(定期账单有续费日,日历里没有);与**家务** 🟡(家务有 cadence,不进日历) |
| ④ 看得见吗 | 邮件↔行程 🟢(两头都看得到);其余不适用 |
| ⑤ 删了会怎样 | 外部导入删了 **无墓碑,下次同步复活** ❌(全仓通病) |

**结论 🟡。** 日程 ↔ 邮件是全仓最好的一条自动关联,可以当模板。

## 财务（finance）

见上文样板。**结论 🟡:确定性的钱路都通了,断的全是钱与「人/附件/记忆」之间。**

> L1–L4(2026-07-30)之后新增:分录三档冻结、对账与差额诊断、statement 端上解析、
> 退款配对、分摊、凭证。这些**没有改变**上面那张表的判定 —— 它们做的是**钱内部**的
> 准确与可追溯,而不是钱与别的板块的连接。财务这一格要转 🟢,仍然要等
> 「BankTx/Expense 有代表节点进图」。

## 物品 / 收纳（inventory）

| 问 | 答 |
|---|---|
| ① 进什么 | **物品就是 LifeNode** ✅(`inventory.ts:274`,`type:'object'`,tags `收纳`) |
| ② 去重 | **无 `externalId`** ❌ —— 同名物品加两次就是两条,重复导入会堆 |
| ③ 关联谁 | 与**财务** 🟡(拍小票既落 object 又落 Expense,但 `sourceRef` 是指纹不是 node id → 两边不可导航);与**美味** 🟢(pantry 复用同一套 object 节点,做得对);与**衣橱** 🟢(同为 `object` + `收纳` tag);与**记忆** 🟢(它本来就在图里) |
| ④ 看得见吗 | 🟢 |
| ⑤ 删了会怎样 | 删节点不清附件(物品照)❌ |

**结论 🟡。** 主要欠账是**幂等键**。

## 衣橱（wardrobe）

| 问 | 答 |
|---|---|
| ① 进什么 | **衣物就是 LifeNode** ✅(`wardrobe.ts:120`,`type:'object'`,tags `收纳`+`衣橱`),照片挂 `node.assets` ✅ —— 附件做法比财务对 |
| ② 去重 | **无 `externalId`** ❌ |
| ③ 关联谁 | 与物品 🟢(同一套 object);与**财务** ❓ 衣物无价格字段 → 买衣服的钱和这件衣服连不上;与**天气/日程** 🔴(穿搭建议不看日程) |
| ④ 看得见吗 | 🟢 |
| ⑤ 删了会怎样 | 附件挂在 `node.assets` 上,删节点仍不清本地文件 ❌ |

**结论 🟡。** 财务那格里标 ❓ 的「衣橱有没有价格」——**核实了:没有**。

## 关系 / People（relationships）

| 问 | 答 |
|---|---|
| ① 进什么 | 联系人是 LifeNode ✅(`manual-contacts.ts:88`,`type:'person'`);互动记录走 `person-records`(自建表) |
| ② 去重 | 手动联系人**无 `externalId`** ❌;`person-records` 有自增 id ⚠️ |
| ③ 关联谁 | 与**财务** 🟡(`tx-annotations.people` 存人名 key,只有 FinanceTab 读 → Linda 的关系页看不到这笔钱);与**健康** 🟡(P2 打通,但走 overlay);与**家务** 🟢(按邮箱自动配);与**记忆** 🟡 —— 人是节点,但「谁出现在这条记忆里」多数走 tag 而不是 `relations` |
| ④ 看得见吗 | 🟡 —— 财务/健康那两条都是单向可见 |
| ⑤ 删了会怎样 | 删人不清 `person-records` ❌ |

**结论 🟡。** 「人」是全仓被引用最多的实体,却是被 overlay 连得最多的一个。

## 资产 / 车（tesla）

| 问 | 答 |
|---|---|
| ① 进什么 | `ManualAsset` 自建表;照料记录自建表 |
| ② 去重 | 资产 id ✅;照料记录自增 id ⚠️(同一张发票传两次两条) |
| ③ 关联谁 | 与**财务** 🟢 —— `addManualEntry({assetId, assetCostKind})` + `expenseId` 回写,**双向、删不留孤儿账**,是全仓做得最对的一条业务关联;与**记忆** 🔴(资产不在图里) |
| ④ 看得见吗 | 财务↔资产 🟢;记忆 🔴 |
| ⑤ 删了会怎样 | 照料记录的 `expenseId` 回写让账能一起清 ✅ |

**结论 🟡。** 横向连接是模板级的,纵向(进图)缺席。

## 生活模型（living）

| 问 | 答 |
|---|---|
| ① 进什么 | 云 AI 产出的画像,自建表 + 付费门 |
| ② 去重 | 覆盖式(一份画像),不堆 ✅ |
| ③ 关联谁 | 读所有面 —— 消费者 |
| ④ 看得见吗 | 🟢 它自己就是一个视图 |
| ⑤ 删了会怎样 | 覆盖式,无孤儿 |

**结论 🟢**(作为消费者)。⚠️ 但它是云 AI 输出,受扩张协议第 5 条约束:**输出必须过校验**。

## 运维（admin）

| 问 | 答 |
|---|---|
| ① 进什么 | 成本/用量指标,自建表。**内部机制信号,按扩张协议第 8 条不该进记忆** ✅ |
| ② 去重 | 按日聚合键 ✅ |
| ③ 关联谁 | ⚪ 有意不连 |
| ④ 看得见吗 | 不适用 |
| ⑤ 删了会怎样 | 不适用 |

**结论 ⚪。** 有意隔离,理由成立,不要「顺手打通」。

## 家务（chores）

| 问 | 答 |
|---|---|
| ① 进什么 | 家务/派工/账本,`lib/family/*` 自建表 + 云端 families 表 |
| ② 去重 | 家务 id + cadence 展开键 ✅ |
| ③ 关联谁 | 与**关系** 🟢(按邮箱自动配人);与**财务** ⚪ **有意不连** —— `finance-sources.ts:4` 明写「家务零花钱故意不进这里」;与**日程** 🟡(cadence 不进日历);与**记忆** 🔴 |
| ④ 看得见吗 | 家庭板内 🟢;跨面 🔴 |
| ⑤ 删了会怎样 | 冲正账已有(软删 + 重算)✅ |

**结论 🟡。** 唯一一个把「有意不连」写在代码注释里的板块,值得学。

## 美味 / 做饭（cooking）

| 问 | 答 |
|---|---|
| ① 进什么 | 一餐是 LifeNode ✅(`meals.ts:36`,`type:'note'`,tag 一餐);采购清单/心愿也进图;库存复用物品的 object 节点 ✅ |
| ② 去重 | **全部无 `externalId`** ❌ —— 同一餐记两次两条 |
| ③ 关联谁 | 与**物品/收纳** 🟢(共用 object,做得对);与**财务** 🔴 —— `addMeal()` **没有金额字段**,永远进不了 `receiptMatchCandidates`;与**健康** 🔴(吃了什么和身体指标不相连) |
| ④ 看得见吗 | 🟢(在图里) |
| ⑤ 删了会怎样 | 无跨面孤儿 |

**结论 🟡。** 「吃」在图里,但既不连钱也不连身体 —— 而这两条正是它最该连的。

---

# 16×16 整合矩阵

行 = 来源,列 = 目标。取**两头都能看到**的最弱状态(A 双向可见才 🟢)。
对角线不填。`—` = 不该连(没有业务含义)。

|  | 回看 | 成长 | 蒙太 | 健康 | 运动 | 时间线 | 日程 | 财务 | 物品 | 衣橱 | 关系 | 资产 | 生活 | 运维 | 家务 | 美味 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| **回看** | — |🟢|🟢|🟢|🔴|🟢|🟢|🟢|🟢|🟢|🟡|🔴|🟢|—|🔴|🟢|
| **成长** |🟢| — |—|🟡|🟡|🔴|🟡|—|—|—|🔴|—|🟢|—|—|—|
| **蒙太** |🟢|—| — |—|—|🟢|—|—|🟢|🟢|🟡|—|🟢|—|—|🟢|
| **健康** |🟢|🟡|—| — |🟡|🟡|🟡|🔴|—|—|🟡|—|🟢|—|—|🔴|
| **运动** |🔴|🟡|—|🟡| — |🔴|🟡|—|—|—|🔴|—|🟢|—|—|🔴|
| **时间线** |🟢|🔴|🟢|🟡|🔴| — |🟡|🟡|🟢|—|🟡|—|🟢|—|—|🟢|
| **日程** |🟢|🟡|—|🟡|🟡|🟡| — |🔴|—|—|🟡|—|🟢|—|🟡|🟡|
| **财务** |🟢|—|—|🔴|—|🟡|🔴| — |🟡|❌无价|🟡|🟢|🟢|—|⚪|🔴|
| **物品** |🟢|—|🟢|—|—|🟢|—|🟡| — |🟢|🟡|🟡|🟢|—|—|🟢|
| **衣橱** |🟢|—|🟢|—|—|—|🔴|❌无价|🟢| — |🟡|—|🟢|—|—|—|
| **关系** |🟡|🔴|🟡|🟡|🔴|🟡|🟡|🟡|🟡|🟡| — |—|🟢|—|🟢|🟡|
| **资产** |🔴|—|—|—|—|—|—|🟢|🟡|—|—| — |🟢|—|—|—|
| **生活** |🟢|🟢|🟢|🟢|🟢|🟢|🟢|🟢|🟢|🟢|🟢|🟢| — |—|🟢|🟢|
| **运维** |—|—|—|—|—|—|—|—|—|—|—|—|—| — |—|—|
| **家务** |🔴|—|—|—|—|—|🟡|⚪|—|—|🟢|—|🟢|—| — |—|
| **美味** |🟢|—|🟢|🔴|🔴|🟢|🟡|🔴|🟢|—|🟡|—|🟢|—|—| — |

## 从整张表读出来的三件事

**一、🔴 全部集中在「自建表 ↔ 记忆图」这一条缝上。**
成长、运动、财务(交易/支出/资产)、家务 —— 这四家的东西不在图里,于是它们与
记忆/问一问/时间线/回看之间**必然**是 🔴。修法只有一条,而且财务月报已经演示过了:
**给自建表加一个代表节点**。不是把几千条流水塞进图,是塞一条「2026 年 7 月财务月报」。

**二、🟡 全部是「单向可见」。**
旅行→财务、健康→关系、财务→人、财务→附件……都是一头写了、另一头不知道。
R1 的 `linkNodes()` 已经把**机制**修好(一次读写两边、反向关系自动推),剩下的是
把这些 overlay 逐个改成 A 类关联。改一个就绿一格,互不依赖,可以慢慢来。

**三、两条应该被抄的模板。**
- **资产 ↔ 财务**:`assetId` + `expenseId` 双向回写,删不留孤儿账。业务关联的标准答案。
- **日程 ↔ 邮件**:`plan-links.ts` 只认确定性锚点(航班号/PNR/日期交集),命中才连,
  幂等可重跑,无 key 也能工作。自动关联的标准答案。

## 优先级(按「改一次绿几格」排)

| 顺序 | 动作 | 绿几格 | 为什么排这 |
|---|---|---|---|
| 1 | 给**成长 / 运动 / 家务**各加一条月度小结节点 | 12 格 🔴→🟢 | 照财务月报抄,工作量最小、覆盖面最大 |
| 2 | `tx-annotations.people` 改成 `relations`(人 ↔ 交易) | 4 格 | 「人」是被引用最多的实体 |
| 3 | 给 `addMeal` / 衣橱加**金额**字段 | 4 格 | 打通「吃/穿」→ 钱,顺带让小票配对能用 |
| 4 | 物品 / 衣橱 / 一餐 / 心情补 `externalId` | 去重 | 不绿格,但挡住「重复导入堆记忆」 |
| 5 | 删除时清附件 + 外部导入留墓碑 | 全局 | 全仓通病,不是某一格的事 |

# 扩张协议 · 新板块 / 新功能接入清单

> **目的：下一个功能不用再进这份审计。**
> 新写一个板块、一个连接器、一个 AI 能力时，逐条过。任何一条答不上来，就是在制造下一个 🟡。

### 1. 你的东西是 LifeNode 还是自己的表？

**默认应该是 LifeNode。** 只有满足以下之一才可以自建表：

- 数据量级会压垮记忆图（如银行流水几千条）；
- 它是纯派生视图（可从别处重算）；
- 它是凭证/密钥类（绝不该进记忆）。

自建表的，**必须同时**在记忆图里留一个「代表节点」（像月报那样），否则这个板块的东西
在记忆库、问一问、时间线里全部隐形——这是所有 🔴 的共同成因。

### 2. 幂等键

写节点必须带 `emailId` / `notionPageId` / `externalId` 之一
（`lib/life-domain/ingest-node.ts` 的 `externalKey()` 只认这三个）。
**自造去重逻辑要在 `scripts/connector-idempotency.test.mjs` 里登记**，否则下次重构会静默失效。

格式约定：`externalId: '<源>:<该源的稳定 id>'`，例如 `weread:12345`、`toggl:weekly:2026-07-30`。

### 3. 关联走 A，不走 B

要跟别的板块连，**优先把两头都变成 LifeNode，然后写 `relations`**。

- **一律走 `linkNodes(fromId, toId, relation)`**（`lib/portal/life-graph.ts`，R1 2026-07-30）。
  它一次读写把两边写完、自带去重、按 `RELATION_INVERSE` 自动推反向关系、targetId 不是
  真节点 id 就返回原因。**手写 `relations: [...]` 已被 `scripts/node-links.test.mjs`
  反向扫描禁掉**（创建节点那一刻除外——那时对方还没有 id）；
- `targetId` **必须是节点 id**，不许是人名/邮箱/任何业务键。
  （旧反例：`life-graph.ts` 的 `owned_by` 曾存人名，该关联永远渲染不出来。R1 已修，
  存量数据由 `repairDanglingRelations()` 在图水合时自动修好。）
- 新关系要**双向成对**：加进 `RELATION_INVERSE`（`part_of_plan` ↔ `plan_item`，
  `confirmed_by_email` ↔ `confirms_plan`）。不加的话走对称，多数时候也对；
- 新关系名必须加进 `MemoryNodeDetail.tsx` 的 `REL_LABEL`——**不在表里的关系会被静默吞掉**，
  不报错、不显示（现存反例：`owned_by` / `family` / `producer_to_consumer`）。

确实必须用 overlay（B）时，**必须回答：另一头在哪里看？** 答不上来就不要写。

### 4. 自动关联要「可辩护」，不要 AI 猜

模板是 `lib/portal/plan-links.ts`：只认确定性锚点（id 交集 / 编号一致 / 日期吻合），
命中才连，幂等可重跑，无 key 也能工作。

不确定的一律**建议 + 人确认**（模板：`receipt-match.ts` + `FinanceTab.tsx:759-771` 的
「关联 / 不是」两颗按钮，「不是」进否决记忆永不重复推荐）。

### 5. 云 AI 的输出必须过校验

`/api/portal/analyze` 的 `result.nodes` 目前**零校验**直接透传给客户端 `ingestLifeNode`
（`analyze/route.ts:524` → `CameraSheet.tsx:861` 的 `as LifeNode['type']` 强转）。
新增任何「AI 产出记忆」的路径，type 必须过白名单，认不出的落 `note` 并把原值留在 `rawType`。

### 6. 附件

一张照片派生多条记忆时，附件/指纹/地名/云资源要给**全部**节点，不能只给第 0 条
（现存反例：`CameraSheet.tsx:907/911/928/960`）。

### 7. 删除

新表要回答：删记忆时它清不清？删它时附件清不清？外部导入的删了会不会被下次同步复活？

### 8. 内部机制信号不进记忆

反馈、埋点、缓存这类内部信号走 `createSignal` 会**变成用户可见的记忆**
（现存反例：`retrieval-feedback.ts:57`、`signal-feedback.ts:76`、`feedback-log.ts:95`
——点一次「不是这个」就多一条叫「检索反馈:不是这个」的笔记）。
新增内部信号必须标记为内部并在 `memory-visibility.ts` 拦掉。

---

## 变更记录

| 日期 | 内容 |
|---|---|
| 2026-07-30 | 建档。三套机制 + 四色 + 五问 + 财务样板 + 扩张协议 |
| 2026-07-30 | R2:补齐其余 15 格 + 16×16 整合矩阵 + 按「改一次绿几格」排的优先级 |
